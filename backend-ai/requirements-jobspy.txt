# python-jobspy 1.1.82 wheel metadata pins NUMPY==1.26.3 and regex<2025, which conflict with
# scipy (numpy>=1.26.4) and transformers (regex>=2025.10.n). Runtime is fine with looser versions.
# Install after requirements.txt:
#   pip install -r requirements.txt
#   pip install --no-deps -r requirements-jobspy.txt
python-jobspy==1.1.82
